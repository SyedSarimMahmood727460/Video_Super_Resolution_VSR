[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cuda-vsr-mahti"
version = "0.2.0"
description = "Custom CUDA-kernel video super-resolution pipeline for Mahti"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = []
